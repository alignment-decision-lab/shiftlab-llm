models:
  name: distilgpt2

dataset:
  name: "OpenLLM-France/wikisource"
  config: null
  format: "text"
  text_column: "text"
  cleaning: "wikisource"

training:
  epochs: 30
  batch_size: 16
  learning_rate: 0.00001
  dataset_size: 7000
  val_split_ratio: 0.1
  max_length: 128
  seed: 42

diagnostic:
  alpha: 0.2
  beta_train: 0.1
  betas_test: [0.0, 0.1, 0.3, 0.5, 0.8, 1.0]
  train_size: 5000
  test_size: 1000
